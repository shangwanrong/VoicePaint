// 反馈系统（占位，PR07实现）
class FeedbackSystem {
  speak(text) {
    console.log('FeedbackSystem: speak', text);
  }

  showStatus(key, value) {
    console.log('FeedbackSystem: showStatus', key, value);
  }
}
